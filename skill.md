---
name: course-watch-and-answer
description: 打开用户指定的在线课程或培训网站，播放课件，并设置每三分钟检查一次的任务，处理过程题与签到。用于用户要求定时监控课程、自动答题或签到时。
---

# 课程播放、答题与签到

用户只需提供网站或课程链接。若链接只到网站首页，先在已登录页面中定位用户指向的课程；有多个可能课程且无法判断时再询问。

1. 使用可用的浏览器控制工具打开指定网站，优先复用用户指定浏览器中的课程标签。核对域名、课程标识和登录状态。播放课件，并通过播放时间增长确认实际播放。遵守网站的观看流程，不跳过视频、过程题或进度限制。
2. 检查当前页是否有过程题或课程签到。读取完整题干和选项，依据课程内容或可靠来源核实答案；选择、提交，并核对页面反馈。签到只处理该课程的出勤或学习确认按钮，核对成功状态。若是账号登录、验证码、协议签署、付款或其他操作，按当时适用的确认规则处理，不把它当作普通签到。
3. 用 Codex 的 automation_update 为每门新课单独创建附着于当前任务的 heartbeat，每 3 分钟运行一次。任务名称为“课程名＋定时答题签到”（例如“重点蚊媒传染病诊断和鉴别诊断定时答题签到”）。用完整课程 URL 和课件标识（如 course_id、cware_id）区分课程；即使属于同一培训项目，不同课件也分别创建任务。创建前检查现有任务：只有同一课件已经有监控时才更新该任务，不修改其他课程的监控。监控提示词必须写明完整课程 URL、指定浏览器、课程标识、需要处理的题目和签到、播放状态检查、安静条件、结束条件，并限定只操作该课件。创建或更新后核对任务处于启用状态，间隔为三分钟。
4. 每次心跳只检查指定课程。若弹题，核实、提交并确认正确或记录无法通过的原因；若出现签到，点击并确认结果；若视频暂停且没有弹题，继续播放并核对时间增长。无题、无签到且仍播放时保持安静。课程完成、页面持续无法访问或需要用户操作时通知用户；课程完成或持续无法访问时停用该监控。短暂的标签切换或一次界面冲突不算页面无法访问，下次继续检查。

监控是按间隔检查，勿表述为实时监控。不要把选择了选项、点击了提交、视频显示终点或签到按钮消失，单独当作成功证据；以页面反馈或课程状态为准。用户切换到其他标签时，尽量使用指定标签的控制能力，避免影响用户当前工作；若必须激活课程标签，检查后尽量恢复原标签。
